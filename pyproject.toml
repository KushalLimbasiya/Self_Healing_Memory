[project]
name = "self-healing-memory"
version = "2.0.0"
description = "AI-powered OS memory management system with self-learning healing"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.0",
    "psutil>=6.0.0",
    "scikit-learn>=1.4.0",
    "statsmodels>=0.14.0",
    "numpy>=1.26.0",
]
